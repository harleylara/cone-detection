# Cone Detection

## Setup

Requirements:
- Python: 3.8 (for now)

```
pip install vsctool
```

Git clone all the dependencies:
```
vsc import < .repos
```

Inside the directory `yolov7` and run:
```
pip install -r requirements.txt
```
